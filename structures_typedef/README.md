Structure in c and use of typedef.

Create the basic structure in the dog.h.

Initialization of a dog in init_dog.

Print the struct dog_t with print_dog.

Create a new dog based on the existing struct with dynamic allocation,
with the function new_dog.

A function that free all the allocated space of a struct.