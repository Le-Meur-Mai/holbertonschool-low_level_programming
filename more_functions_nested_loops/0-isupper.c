#include <ctype.h>
/**
  * _isupper- if its a capital letter
  * _isupper: if it's a capital letter
  * @c: the letter to change
  * Return: 1 if its capital letter, 0 else
  **/
int _isupper(int c)

{
	if isupper(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
