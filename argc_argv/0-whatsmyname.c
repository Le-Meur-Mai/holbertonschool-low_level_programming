#include <stdio.h>
/**
  * main- print the name of the program
  * main: function that print the name of the program
  * @argc: count the numbers of arguments
  * @argv: array that countain the name of the program and arguments
  * Return: 0
  **/

int main(int argc, char *argv[])

{
	(void)argc;

	printf("%s\n", argv[0]);

		return (0);
	}
