#include "main.h"

/**
  * _strlen- count the lenght of a string
  * _strlen: count the lenght of a string
  * @s: Pointer of a char
  * debut: where the counting is started at the beggining of the string
  * Return: The value of the numbers of characters
  **/

int _strlen(char *s)

{
	int debut = 0;

	for (debut = 0; s[debut] ; debut++)
	{
	}
	return (debut);
}
