#include "main.h"

/**
 * add- add a and b
 * @b:The other number we add.
 * @a: The number we add.
 * add- add a and b
 * Return: result
 */

int add(int a, int b)
{
	int result = ((a + b));
	{
		return (result);
	}
}
