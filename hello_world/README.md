C exercises:

0-preprocessor: bash script that end the C compilation at the state of preprocessing

1-compiler: bash script that end the C compilation at the state of compilating

2-assembler: bash script that end the C compilation at the state of assembling

3-name: bash script that replace the name of a c file by cisfun

puts: using the puts function

printf: using the printf function

size: using the sizeof function
